<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools" android:id="@+id/app_navigation"
            app:startDestination="@id/moviesFragment">
    <fragment
            android:id="@+id/moviesFragment"
            android:name="com.demo.mvilivedatacoroutines.ui.main.movie.MovieFragment"
            android:label="fragment_movies"
            tools:layout="@layout/movies_fragment">
        <action
                android:id="@+id/detailAction"
                app:destination="@id/movieDetailFragment"/>
    </fragment>
    <fragment
            android:id="@+id/movieDetailFragment"
            android:name="com.demo.mvilivedatacoroutines.ui.main.moviedetail.MovieDetailsFragment"
            android:label="fragment_movie_detail"
            tools:layout="@layout/movie_detail_fragment">
    </fragment>
</navigation>